name: solid_lints_alphabetize_by_type_test
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  solid_lints:
    path: ../../
  test: ^1.20.1
