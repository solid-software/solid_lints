name: solid_lints_test
description: Test project for solid_lints rules
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  solid_lints:
    path: ../
  test: ^1.20.1
